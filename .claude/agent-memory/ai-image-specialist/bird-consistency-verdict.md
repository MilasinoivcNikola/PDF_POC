---
name: bird-consistency-verdict
description: Birds (cockatiel) DO hold on-model under Approach A + mixed PRODUCTION_QUALITY across all 8 Story-7 scenes — the flagged-risk species passed, no re-pick needed
metadata:
  type: project
---

The Story-7 "Welcome Home" bird sample (the catalog's first feathered sample, flagged as the one mild consistency risk — birds anchor worse than mammals: busy plumage, less facial structure) **held on-model** under Approach A + the locked mixed `PRODUCTION_QUALITY` (HIGH cover / MEDIUM 7 interiors / LOW reference), one paid run (~$0.72), zero re-pick.

**What carried it:** a *distinctive* specimen. `uploads/sample-photos/bird.jpg` is a grey **cockatiel** with a sunny yellow face/crest, bright **orange cheek patches**, white wing flashes. Those orange cheeks + yellow crest are a strong, repeatable anchor — they rendered correctly in all 7 bird-present scenes (cover, choosing, drive-home, first-night, learning, now-ours, the `welcome-belong` close-up). The figure-free `welcome-before` correctly had no bird. **Takeaway: for hard-anchor species (birds), pick a high-saturation/high-contrast marking pattern — the bolder the markings, the better the anchor.**

Minor non-blocker noted: `welcome-learning` rendered 3 birds (2 on-model cockatiels foreground + a darker flying bird) — a reasonable playful-scene interpretation, both foreground birds on-model. Not drift.

See [[story7-mixed-set-imagery]] (which left the pet-consistency eyeball as "NOT yet live-validated" — this run closes that gap for the species). Approach A + distinctive-specimen rule mirrors [[story1-high-tier-verdict]]'s distinctive-boxer finding.
