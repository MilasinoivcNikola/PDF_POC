## 2026-06-13 — Story 6 (PR-26): Wizard, Storefront & Order Intake — creatable + sellable

**Branch:** `feature/story6-wizard` → `main` (`022da44`, merge `fe9fb73`) · Milestone 10 (Story 6 — "While You're Still Here, [PET_NAME]"), Phase 2 (PR 2 of 2 — **completes Story 6 / Milestone 10**) · Craft Area 3 (nextjs-ui-builder).

The feature-22/24 (Story 4/5) analog for Story 6, with one structural first: this is the **first narrative-spread storefront book** (the prior sellable products were Story 1's narrative spread plus the three single-column letters). PR-25 authored the Story-6 text engine, the `"story-6"` registry entry, `STORY_6_STEPS`, the `editable` contract, and the 8-image generation (reference + 7 `tribute-*`); PR-26 is the UI/commerce wiring that makes the **living tribute** both **creatable** (operator wizard) and **sellable** (public storefront + order form) end to end — landing → wizard / order → generate → preview → download — **reusing the whole commerce loop (intake → pay → worker → admin → delivery) by id with zero infra change**. **Stories 1, 2, 4 and 5 stay behaviorally unchanged.**

- **The headline — Story 6 is a narrative book, not a letter (the `isLetter` exclusion).** Unlike the three letter products, Story 6 reuses Story 1's shape: the **`pet` step keeps pronoun + illustration style + breedColor**, the preview renders the **facing-page spread** (not single-column), and it has **8 illustrations** (reference + 7 `tribute-*`), not 14 / 2. The load-bearing change is that the `isLetter` predicate (used in the pet step, `OrderForm`, `BookPreview`, the generate page) **enumerates `story-2 || story-4 || story-5` literally and excludes story-6**, so story-6 falls into the existing narrative/spread `else` and the four existing products' paths are untouched. Every new branch is an additive `isStory6 ? … : …` preserving existing strings verbatim.
- **Draft bridge + validation** — `lib/session/draft.ts`: `missingRequiredFieldsStory6` / `draftToSessionStory6` / `isStory6Draft` (+ the dispatchers `missingRequiredFieldsForDraft` / `draftToSessionForDraft`, whose PR-25 "not wired (PR 26)" throws are removed). **Required set (8): `petName`, `species`, `breedColor`, `ownerNames`, `ageOrStage`, `favoriteRitual`, `favoriteActivity`, `photo`** (`photo` reported last). The optional-with-fallback fields `quirks` / `stillLoves` / `favoriteSpots` / `sleepingSpot` are stored as `""` when blank (PR-25's variant layer treats `""` as the fallback trigger — dropping the key would break the stock-line fallback), while `ownerMessage` / `nicknames` / `dateAdopted` are dropped-when-blank as absent keys. Keeps pronoun + style; no `child` / `deathType` / `beliefFrame`. The cross-product invariant: a blank-`quirks` Story-6 draft is **accepted** where a Story-2/4 one rejects — proving the dispatch hit the right branch. `app/(operator)/api/session/route.ts` — `validateStory6` + per-`storyType` dispatch (house JSON shape + `isSafeSessionId` guard preserved; Story 1/2/4/5 branches byte-identical).
- **Wizard (operator).** `WizardProvider` `DraftPatch` union += `Story6Memories` / `Story6Toggles` (generic per-group merge handles it). Flow `upload → pet → tribute → tone → generate` ("Step NN of 05"): the **`pet`** step (story-6 variant) keeps pronoun/style/breedColor + the new **`ageOrStage`**; a new **`tribute`** step collects owner names + present-tense day-to-day life fields ("celebrate, never pre-bury"); the **`tone`** step is `transitionFrame` (`still-here` default / `road-ahead`) + `otherPetsInHome` only — no death/belief/gift/living-memorial toggles. `illustrationLabels.ts` — `TRIBUTE_ILLUSTRATION_SLOTS` (`reference` + 7 `tribute-*` = 8) + present-tense labels + a `== getStory("story-6").illustrationSlots` registry-drift guard (strips the leading `reference` anchor, per the Story-1 pattern); `GenerationProgress.tsx` — story-6 subtitle + ~$0.05 (8-image) footer.
- **Storefront / catalog.** `app/(public)/page.tsx` + `page.module.css` — a fifth landing card framed as **the living tribute** ("Still here · a living tribute · the one book made before goodbye"), set apart from the four after-loss titles with a sage `chooserCardLiving` accent; five-keepsake lede/CTA. `lib/catalog/products.ts` — `PLACEHOLDER_STORY_6_PRICE_USD = 3200` ($32, top of the $32–35 band) + `buildProduct("story-6-tribute", "story-6", {...})` with master-template marketing copy (paras 1–3, memorial paragraph dropped); **`illustrationCount` derived from the registry (= 7)**, never hardcoded. `OrderForm.tsx` — `Story6Fields` + story-6 seeding/dispatch (`isStory6Draft`, `newDraft("story-6")`, `draftToSessionForDraft`), keeping pronoun + style. `public/samples/story-6-tribute/{tribute-cover,tribute-page-3}.jpg` (800×800, `sips` q78, no new dep, from PR-25's on-disk `feat25-tribute-otis` Low run). `.env.local.example` += `LEMONSQUEEZY_VARIANT_STORY_6_TRIBUTE` (non-secret; the generic `LEMONSQUEEZY_VARIANT_<PRODUCT_ID>` checkout convention resolves it with no code change).
- **Preview / delivery.** `BookPreview.tsx` — `isLetter` confirmed to exclude story-6 → **facing-page spread** path; story-6 fallback filename `While-Youre-Still-Here.pdf`. `lib/delivery/email.ts` — story-6 falls to the "book" noun (correct, no diff needed). Inline edit + per-page regenerate are registry-driven (PR-25 `editable`) — no route change. **Type-only commerce touch:** `Order.inputs` (`lib/order/types.ts`) + `OrderRow.inputs` (`lib/order/store.ts`) + `AnySession` (`lib/session/disk.ts`) widened to admit `Story6Session` (the mandatory reuse-guarantee implication, as PR-22/24 did for Story 4/5; no mapper logic change). `lib/runtime/surface.boundary.test.ts` already banned `lib/ai/story6-prompts` (PR-25) and needs no new public entry — the public storefront/order graph stays engine-free.

**Reviews (code PASS · commerce-security PASS · context IN SYNC).** Code review: **PASS**, no blockers — all five load-bearing invariants verified (the `isLetter` exclusion + additive ternaries keep Stories 1/2/4/5 untouched; narrative-spread + pronoun/style; `draftToSessionStory6` no-MergeError on blank optionals; type-only `Order.inputs` widen; boundary intact). Non-blocking carry-forwards: the name-less `fallbackFilename("story-6")` (identical accepted Story-5 pattern, only on a `Content-Disposition` parse failure) and the operator-step/order-form field duplication (established Story-2/4/5 split). Commerce security: **PASS ("ship it")**, no HIGH/MEDIUM/LOW — Story 6 inherits the proven posture **by construction** (the public `/api/order` route untouched + product-agnostic; `NewOrderInput` structurally excludes `status`/`pdfKey` → no spend-before-payment/status-injection; server-minted order id → no IDOR; public page graph engine-free + service-role-free; non-secret variant id; no PII/secret logged; **both sample JPEGs parsed segment-by-segment → PII-clean** — sRGB + 800×800 dims only, no GPS/Make/Model/serial). Context audit: **IN SYNC**, no edits — every new fact is carried by a generic convention (`LEMONSQUEEZY_VARIANT_<PRODUCT_ID>`, "one Product per storyType / illustrationCount derived", the PR-22-broadened "living titles" roadmap header, the master-template copy/$32/tone match). One non-blocking nice-to-have recorded: `new-book-playbook.md`'s "reuse guarantee" should note that every *sellable* book widens three `inputs` type-unions (now PR-22→24→26) — a later-branch doc touch.

**Live QA (PASS 5/5, ~$0.036 — no fresh book intended; one halted Generate reached 4 Low images ≈$0.024 + one sanctioned repaint ≈$0.012).** Driven in a real browser (Playwright MCP) + curl/psql against the **local `supabase start` stack** (all Supabase env vars overridden inline; cloud project never touched — 0 `supabase.co` traffic), operator dev on `:3100` / public on `:3200`, reusing PR-25's on-disk `feat25-tribute-otis` book. (1) Landing offers Story 6 as the living tribute, set apart; `/books` lists `story-6-tribute` at **$32**, detail shows **"7 painted from your photo"** (registry-derived); Stories 1/2/4/5 cross-check 13/2/2/2 at $29; samples 200. (2) Wizard `upload → pet → tribute → tone → generate`: the **pet step keeps pronoun + style + breedColor** + `ageOrStage`; `tone` = `transitionFrame` + `otherPetsInHome` only; **8 required gates** fire; mid-wizard refresh kept the **same draft id** + values; Generate wrote a valid `Story6Session` (no `child`/death/belief) → progress showed the **8-slot checklist** (reference + 7 `tribute-*`, "~$0.05 per book"), not 14/2; **Strict-Mode double-POST → single run** (TOCTOU held). (3) Public order form → a **`pending_payment`** order in local Supabase (`story_type: story-6`, full `Story6Session` inputs, photo in `order-photos`, **server-minted id distinct from the client `inputs.id`** — IDOR-safe) — no generation, no charge; `missing_favorite_activity` / `invalid_email` / `product_mismatch` reject server-side; `/api/checkout` 503 → graceful fallback. (4) Preview rendered the **facing-page spread** (`.spread × 4, .preview-single = 0`) with the real reference-anchored Otis watercolors across all 8 pages; **Download → `While-Youre-Still-Here-Otis.pdf`** (8 pages, all Letter `[0 0 612 792]`, `%PDF-1.4`); inline `ownerNames` edit re-resolved + persisted, required-blank rejected with disk untouched; per-page regenerate of `tribute-page-3` changed only that PNG (md5), manifest spliced to 8 no dupe. (5) Stories 1/2/4/5 regression — Story 1 `Saying-Goodbye-to-Otis.pdf` 14pg spread, Story 2 `Letter-from-Murphy.pdf` 6pg single-column, Story 4 `If-Otis-Could-Talk.pdf`, Story 5 `Letter-to-Otis.pdf` — the spread/letter split didn't regress the letter books. Cleanup complete; disposable Supabase row + photo removed; canonical fixtures (`feat25-tribute-otis`, `b41b8df0`, `dc34eb11`/Murphy, `feat21`, `feat23`) intact; cloud project never written; both dev servers stopped.

**Gates:** `npm run build` ✓ (`/books/story-6-tribute` + `/order/story-6-tribute` SSG; storefront static, operator dynamic; public graph engine-free) · `npm run test:run` ✓ (**1450 tests, +93** — `lib/session/draft.test.ts` Story-6 block + the `resolveStory6(draftToSessionStory6(...))` round-trip smoke; `/api/session` Story-6 validation branches disk-mocked; `lib/catalog/products.test.ts` registry-derived `illustrationCount` = 7 guard; `components/wizard/illustrationLabels.test.ts` `TRIBUTE_ILLUSTRATION_SLOTS` drift guard; the new `lib/story/story6/editable-fields.test.ts`) · `npx tsc --noEmit` ✓ clean · public-boundary test ✓ (7/7) · code review **PASS** · commerce security **PASS** · context audit **IN SYNC** · `qa` **PASS** (5/5).

**Milestone 10 (Story 6) complete — all five products now run the full craft + commerce loop end to end: landing picker → wizard / order → generation → preview → print-quality PDF → (paid) approve → delivery, by id with no infra fork. Story 6 is the catalog's one *living* keepsake (made before loss) and the first narrative-spread storefront book.** **Manual go-live items still pending across the commerce build (not code):** the Lemon Squeezy store setup (now incl. a fifth manual-fulfilment product/variant + `LEMONSQUEEZY_VARIANT_STORY_6_TRIBUTE`), final Story-6 price confirmation, a live test-mode purchase, a live Resend send-test, and the deferred AI-honesty disclosure copy + privacy policy. **Out of scope (dropped per PM):** the memorial re-render / second-life path (no re-order/reopen, no `truth` layout, no `DEATH_TYPE`/`BELIEF_FRAME`); re-send / order-lookup flows (post-MVP). **Carried from PR-20/23/25 (still open, not this PR):** the authored memorial/couple/non-dog/species gap-fill copy flagged for the grief-counselor review; the `new-book-playbook.md` reuse-guarantee `inputs`-union doc touch; the Story-6 `love`-page slots (pages 5 & 6) generate scenes the reused `love` renderer doesn't display (Story-1 parity — PM to decide whether to trim). **Process note:** local Supabase only ([[supabase-local-verify]]); dev servers stopped before the final `npm run build` ([[dev-build-cache-conflict]]); the one sanctioned Low repaint per the standing cost rule ([[qa-low-tier-cost-control]]).
