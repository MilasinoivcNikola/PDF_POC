## 2026-06-12 — Story 5 (PR-24): Wizard, Storefront & Order Intake — creatable + sellable

**Branch:** `feature/story5-wizard` → `main` (`63d64b3`, merge `37e54f8`) · Milestone 9 (Story 5 — "A Letter *to* [PET_NAME]"), Phase 2 (PR 2 of 2 — **completes Story 5 / Milestone 9**) · Craft Area 3 (nextjs-ui-builder).

The feature-22 (Story 4) analog for Story 5: made the **owner→pet companion of Story 2** both **creatable** (the operator wizard) and **sellable** (a public storefront listing + order form), end to end — landing → wizard / order → generate → preview → download — **reusing the whole commerce loop (intake → pay → worker → admin → delivery) by id with zero infra change**. PR-23 authored the text engine + `"story-5"` registry entry + `STORY_5_STEPS` + `editable-fields` + the 2 Premium images; PR-24 is the UI/commerce wiring. **Stories 1, 2 and 4 stay behaviorally unchanged** end to end.

- **The headline — Story 5 merchandised as Story 2's companion, never an either/or.** A fourth landing card sits **beside** Story 2 ("one from them, one from you"); `/books/story-5-letter-to` lists at **$29** with a registry-derived **2-illustration count**; the detail page carries a **bidirectional copy-only cross-link** (Story 5 ↔ "A Letter from [PET_NAME]") via a `COMPANION_PRODUCT_ID` map — **no bundle SKU / combined-price cart** (out of scope, breaks the reuse guarantee). The master template's #1 product risk (cannibalizing Story 2) is mitigated in copy/placement, not by hiding it.
- **Required set = 6, not 7 (the one real divergence from Story 2/4).** `lib/session/draft.ts` — `missingRequiredFieldsStory5` / `draftToSessionStory5` (+ the dispatchers `missingRequiredFieldsForDraft` / `draftToSessionForDraft`, whose PR-23 "not wired (PR 24)" throws are removed). Required: `petName, ownerNames, species, photo, favoriteRitual, favoriteSpots`. **`quirks` is optional-with-fallback** here (unlike Story 2 where it's required) — stored as `""` so the variant layer fires the stock Page-3 line, NOT dropped (dropping the key would break the fallback); `lastGoodDay`/`whatIKeep`/`nicknames`/dates ARE dropped-when-blank. The cross-product invariant locked in tests: a blank-`quirks` Story-5 draft is **accepted** where a Story-2/4 one rejects — proving the per-`storyType` dispatch hit the right branch. `app/(operator)/api/session/route.ts` — `validateStory5` + per-`storyType` dispatch (house JSON shape + `isSafeSessionId` guard preserved; Story-1/2/4 branches byte-identical).
- **Wizard (operator).** `WizardProvider` `DraftPatch` union += `Story5Memories` / `Story5Toggles` (the generic per-group merge already handled it). The `pet`/`generate`/`preview` steps generalize the "letter products" branch to `isLetter` = story-2 ∨ 4 ∨ 5 (drop child/pronoun/style). `owner` reused with an inverse "in your own voice / signed by you" framing. `letter` adds the two new optional fields + makes `quirks` optional, **no `favoriteActivity`**. `tone` gains a new `Story5Tone` (only `deathType` + `beliefFrame` — no gift/living-memorial/new-pet). All via three-way `isStoryX ? … : …` ternaries preserving Story-2/4 strings verbatim. `illustrationLabels.ts` — `NOTE_ILLUSTRATION_SLOTS` (`note-cover`/`note-page-5`) + owner-toned labels + a `== getStory("story-5").illustrationSlots` registry-drift guard; `GenerationProgress.tsx` — story-5 subtitle + ~$0.02 (2-image) footer.
- **Letter-surface (near-free reuse).** `BookPreview.tsx` — `isLetter` single-column + `Letter-to-[PET_NAME].pdf` filename include story-5; `lib/delivery/email.ts` — noun "letter". Preview/download/update-text/regenerate were already registry-driven (feats 14/16/17/19) — **no route fork**.
- **Catalog + storefront.** `lib/catalog/products.ts` — `PLACEHOLDER_STORY_5_PRICE_USD = 2900` + `buildProduct("story-5-letter-to", "story-5", …)` with master-template marketing copy and **registry-derived `illustrationCount`** (= 2, never hardcoded). `OrderForm.tsx` — `Story5Fields` + story-5 seeding/dispatch (`newDraft("story-5")` → `draftToSessionForDraft`, reusing the unchanged public `/api/order` create path). `public/samples/story-5-letter-to/{note-cover,note-page-5}.jpg` — real frames from PR-23's on-disk `feat23-note-otis` Low run, optimized with built-in `sips` (`-Z 800`, q78; ~128/144 KB, no new dep). `.env.local.example` += `LEMONSQUEEZY_VARIANT_STORY_5_LETTER_TO` (non-secret; the generic `LEMONSQUEEZY_VARIANT_<PRODUCT_ID>` checkout convention resolves it with no code change).
- **Type-only commerce touch.** `Order.inputs` (`lib/order/types.ts`) + `OrderRow.inputs` (`lib/order/store.ts`) + `AnySession` (`lib/session/disk.ts`) widened to admit `Story5Session` — the mandatory reuse-guarantee implication, exactly as PR-22 did for `Story4Session` (no mapper logic change). `lib/runtime/surface.boundary.test.ts` already banned `lib/ai/story5-prompts` (PR-23) and needs no new public entry — the public storefront/order graph stays engine-free.

**Reviews (code PASS · commerce-security PASS · context IN SYNC).** Code review: **PASS**, no blockers — confirmed Stories 1/2/4 byte-identical (every touch a `|| story-5` widening or string-preserving three-way ternary), the required-set-6 consistent across client gate + `validateStory5` + bridge, `illustrationCount` registry-derived, the dispatch has no fall-through (`isStory1Draft = !2 && !4 && !5`), and the `Order.inputs` widening type-only/sound. Non-blocking: `fallbackFilename("story-5")` returns name-less `"Letter-to.pdf"` (only on a `Content-Disposition` parse failure; real name comes from the registry); pre-existing `<img>` LCP warnings (repo pattern). Commerce security: **PASS ("ship it")** — the new product opened no hole; Story 5 inherits the proven posture **by construction** (the product-agnostic `/api/order` route untouched, `NewOrderInput` structurally excludes `status`/`pdfKey`, the assembler reconstructs a fresh whitelisted session → no spend-before-payment / status-injection / IDOR), the public page graph stays engine-free + service-role-free, the boundary test bans `story5-prompts` non-vacuously, the variant id is correctly non-secret, **both committed sample JPEGs are PII-clean** (parsed segment-by-segment), no PII/secret logged. The second full confirmation of the storefront posture-inheritance recipe. Context audit: **IN SYNC**, no edits — the roadmap header already covers another **memorial** title (Story 5 adds no new category, unlike Story 4's celebration), the env-var convention + `Order.inputs` union are both covered by generically-worded passages (same supersession PR-22 established), and the product matches the master template ($29, `Letter-to-[PET_NAME].pdf`, the 6-field set, companion merchandising satisfied, bundle out of scope).

**Live QA (PASS 5/5, $0 — no generation).** Driven in a real browser (Playwright MCP) + curl/psql/Storage-API against the **local `supabase start` stack** (env-overridden inline; cloud project never touched — 0 `supabase.co` refs), operator dev on `:3100` / public on `:3200`, reusing PR-23's on-disk `feat23-note-otis` book (backed up + restored byte-identical). (1) Landing offers Story 5 beside Story 2 ("one from them, one from you"); `/books` lists it at **$29**; detail shows **"2 painted from your photo"** (registry-derived) + bidirectional cross-links resolve; samples 200. (2) Wizard "Step 01–06 of 06": drops pronoun/style, owner "in your own voice" framing, both new `lastGoodDay`/`whatIKeep` + `deathType`/`beliefFrame` only; **6 required gates fire, `quirks` NOT gated**; mid-wizard refresh kept the **same draft id** + values; Generate wrote a valid `Story5Session` (no `child`/`favoriteActivity`/`livingOrMemorial`/`giftFor`/`newPet`, `images:[]`) → progress showed **"00 of 2"** with the 2 note slots + "~$0.02 per letter"; Strict-Mode double-POST to the paid route blocked. (3) Public order form → a **`pending_payment`** row in local Supabase (`story_type: story-5`, full inputs, photo in `order-photos`, server-minted id distinct from client `inputs.id`) — no generation, no charge; `missing_favorite_ritual` / `invalid_email` / `unsupported_type` / `product_mismatch` reject server-side, validation precedes the Storage write; `/api/checkout` 503 → graceful "we'll email a link" fallback. (4) Preview single-column (**6 `.preview-single`, 0 `.spread`**) with the real Otis cover + figure-free belief wash; **Download → `Letter-to-Otis.pdf`** (6 pages, all Letter `[0 0 612 792]`, `%PDF-1.4`); inline `ownerNames` edit re-rendered + persisted ($0). (5) Story-1 (14 imgs, `Saying-Goodbye-to-Otis.pdf`) + Story-2 (2 imgs, `Letter-from-Murphy.pdf`) + Story-4 (2 imgs, `If-Otis-Could-Talk.pdf`) unchanged. Cleanup complete; disposable Supabase row removed; canonical fixtures intact; both dev servers stopped.

**Gates:** `npm run build` ✓ (`/books/story-5-letter-to` + `/order/story-5-letter-to` SSG; storefront static, operator dynamic; public graph engine-free) · `npm run test:run` ✓ (**1258 tests, +79** — `lib/session/draft.test.ts` Story-5 block + the `resolveStory5(draftToSessionStory5(...))` round-trip smoke test; `/api/session` Story-5 validation branches disk-mocked; `lib/catalog/products.test.ts` registry-derived `illustrationCount` guard; `components/wizard/illustrationLabels.test.ts` `NOTE_ILLUSTRATION_SLOTS` drift guard; the new `lib/story/story5/editable-fields.test.ts`) · `npx tsc --noEmit` ✓ clean · public-boundary test ✓ · code review **PASS** · commerce security **PASS** · context audit **IN SYNC** · `qa` **PASS** (5/5, $0).

**Milestone 9 (Story 5) complete — all four products now run the full craft + commerce loop end to end: landing picker → wizard / order → generation → preview → print-quality PDF → (paid) approve → delivery, by id with no infra fork.** **Manual go-live items still pending across the commerce build (not code):** the Lemon Squeezy store setup (now incl. a fourth manual-fulfilment product/variant + `LEMONSQUEEZY_VARIANT_STORY_5_LETTER_TO`), final Story-5 price confirmation, a live test-mode purchase, a live Resend send-test, and the deferred AI-honesty disclosure copy + privacy policy. **Out of scope (deliberately not built):** the Stories 2 + 5 companion bundle (PM-gated multi-product commerce); the "family" relationship option; re-send / order-lookup. **Carried from PR-20/23 (still open, not this PR):** the authored memorial/couple/non-dog gap-fill copy flagged for the grief-counselor review. **Process note:** local Supabase only ([[supabase-local-verify]]); dev servers stopped before the final `npm run build` ([[dev-build-cache-conflict]]); $0 — no generation ([[qa-low-tier-cost-control]]).

