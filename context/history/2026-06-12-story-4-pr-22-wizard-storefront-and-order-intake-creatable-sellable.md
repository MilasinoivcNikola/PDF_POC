## 2026-06-12 — Story 4 (PR-22): Wizard, Storefront & Order Intake — creatable + sellable

**Branch:** `feature/story4-wizard` → `main` (`d558178`, merge `0733bf4`) · Milestone 8 (Story 4 — "If [PET_NAME] Could Talk"), Phase 3 (PR 3 of 3 — **completes Story 4 / Milestone 8**) · Craft Area 3 (nextjs-ui-builder).

The feature-18/19 analog for Story 4: made the **living/celebration** twin of Story 2 both **creatable** (the operator wizard) and **sellable** (a public storefront listing + order form), end to end — landing → wizard / order → generate → preview → download — **reusing the whole commerce loop (intake → pay → worker → admin → delivery) by id with zero infra change**. PR-20 authored the text engine + `"story-4"` registry entry + `STORY_4_STEPS`; PR-21 generated the 2 Premium images; PR-22 is the UI/commerce wiring. **Story 1 and Story 2 stay behaviorally unchanged** end to end.

- **The headline — `livingOrMemorial` conditional reveal.** A one-SKU/two-audiences product (the "I See Me!" model): the living-vs-memorial choice lives *inside* the Story-4 listing (Story 2 stays a separate grief listing). Collected on the `tone` step (and mirrored in the public `Story4Fields`): default **living** shows only the gift toggle; selecting **memorial** reveals death-type / belief-frame / second-date inline. The "living path persists a garbage enum" trap is **inert** by construction — `draftToSessionStory4` always fills *valid* toggle defaults (never `""`, so no `MergeError`), and `lib/story/story4/variants.ts` reads death-type/belief-frame **only on the memorial branch** (a `resolveStory4` round-trip on both a living and a memorial draft proves no surviving placeholder / no throw).
- **Bridge + validation** — `lib/session/draft.ts`: `missingRequiredFieldsStory4` / `draftToSessionStory4` / `isStory4Draft` (mirrors `draftToSessionStory2` — trim free-text, drop blank `nicknames`/`dateAdopted`/`datePassed` as absent keys, fill toggle/enum defaults incl. `livingOrMemorial: "living"`, set `storyType`/`status:"generating"`/`images:[]`, throw `missing_required_fields`; required set = `petName, ownerNames, species, photo, quirks, favoriteRitual, favoriteSpots, favoriteActivity`). The dispatchers (`missingRequiredFieldsForDraft` / `draftToSessionForDraft`) now route story-4 — PR-20's "not wired yet (PR 22)" throws are removed. `app/(operator)/api/session/route.ts` gains `validateStory4` + per-`storyType` dispatch (house JSON shape + `isSafeSessionId` guard preserved; Story-1/2 branches byte-identical).
- **Wizard** — `WizardProvider` `DraftPatch` unions += `Story4Memories`/`Story4Toggles`; the `pet`/`generate`/`preview` steps generalize the old `isStory2` branch to **`isLetter`** (story-2 ∨ story-4) for routing/field-drop (no child/pronoun/style for letters), with `isStory4 ? <new> : <original>` three-way copy ternaries preserving Story-1/2 strings verbatim (the reviewer **byte-compared** the extracted `Story2Tone` JSX against the original `TonePage` body — identical). `owner` reused as-is; `letter` adds the required **`favoriteActivity`** field; `tone` split into `Story2Tone` (unchanged) + `Story4Tone` (the reveal).
- **Storefront / commerce** — `app/(public)/page.tsx` third landing card ("For a good day → /books/story-4-talk") + a three-product lede/CTA; `lib/catalog/products.ts` `PLACEHOLDER_STORY_4_PRICE_USD = 2900` + `buildProduct("story-4-talk", "story-4", …)` with master-template marketing copy and **registry-derived `illustrationCount`** (= 2, never hardcoded), `lsVariantId` env-resolved server-side; `OrderForm.tsx` `Story4Fields` + story-4 seeding/dispatch (`newDraft("story-4")` → `draftToSessionForDraft`). `public/samples/story-4-talk/{talk-cover,talk-page-4}.jpg` (800px, `sips`, no new dep, pulled from PR-21's Low run). `.env.local.example` += `LEMONSQUEEZY_VARIANT_STORY_4_TALK` (non-secret). `/books` index copy de-staled "Two keepsakes, for two kinds of goodbye" → "A keepsake for every kind of love" + a three-product memorial+celebration lede.
- **Letter-surface** — `components/preview/BookPreview.tsx` `isLetter` (single-column) + filename and `lib/delivery/email.ts` noun include story-4. **`Order.inputs` widened to `Story4Session`** (`lib/order/types.ts` + `lib/order/store.ts` row mapper) — a minimal type-only commerce-surface touch (the intake-carries-inputs-verbatim reuse guarantee), no behavior change to Story-1/2 order rows. All four engine routes (`/api/preview`, `/api/render-pdf`, `/api/update-text`, `/api/regenerate-illustration`) were already registry-driven (feats 14/16/17/19) — **no route fork**.
- **Boundary** — `lib/runtime/surface.boundary.test.ts` adds `lib/ai/story4-prompts` to the engine modules banned from the public graph; the catalog → registry → `lib/story/story-4` chain reaches scene identity via `TALK_SCENE_PAGE_IDS` (declared in the product module, never `lib/ai/*`), so the public storefront/order graph stays **engine-free** (boundary test non-vacuous, SSG build is the live proof).

**Reviews (code PASS · commerce-security PASS · context DRIFT FOUND → resolved on-branch).** Code review: **PASS**, no blockers — all 3 goals confirmed met; the `livingOrMemorial` stale-enum risk ruled inert; Story-1/2 confirmed byte-identical (the `Story2Tone` byte-compare). Commerce security: **PASS** ("ship it") — the Story-4 path is **posture-identical to the proven Story-1/2 commerce path**: the single public write (`/api/order/route.ts`) is **unchanged** + product-agnostic; `NewOrderInput` is a `Pick` that **structurally excludes `status`/`pdfKey`** (a client status is unrepresentable), so no spend-before-payment; server-minted id (no IDOR); the public graph is engine-free; the captured session's `status:"generating"` is never read to drive the lifecycle (generation gates only on the row's `queued`, reachable solely via the signed paid webhook); the two sample JPEGs carry no PII/EXIF (decoded segment-by-segment — empty Exif/IPTC stubs only); `.env.local.example` documents only the non-secret variant id. Context audit: **DRIFT FOUND** (1 item) → fixed on-branch — `commerce-roadmap.md`'s header still said "Pet-memorial focused" while the catalog now sells a primary-use **celebration** product; per PM decision the header was broadened to "Pet-keepsake focused — both memorial … and celebration / living titles." Everything else verified in sync (the generic `LEMONSQUEEZY_VARIANT_<PRODUCT_ID>` env convention already covers the new var; the generic "If Your Pet Could Talk" storefront title matches the Story-1/2 name-drop convention; marketing copy + `$29` track the master template; the `Order.inputs` widening is a mandatory reuse-guarantee implication, not standing-doc drift).

**Live QA (PASS 5/5, ~$0.012 — one Low Story-4 run from the real Generate button; everything else $0 via fixture reuse).** Driven in a real browser (Playwright MCP) + curl/REST against the **local** Supabase Docker stack (env-overridden inline; **0 cloud traffic**), operator dev on `:3100` / public on `:3200`. (1) Landing third card → `/books/story-4-talk`, listed at **$29**, detail shows **"2 painted from your photo"** (registry-derived in the RSC payload: `[2,…]` vs Story-1 `[13,…]`), samples 200. (2) Wizard: **living↔memorial reveal** toggles death-type/belief-frame/second-date both ways; `favoriteActivity` gate fires; mid-wizard refresh kept the **same draft id** + values; Generate wrote a valid `Story4Session` (no `child`, `livingOrMemorial`, `images:[]`) → progress showed **2 slots not 14**; Strict-Mode double-POST held to **one run** (feat-09 TOCTOU guard). (3) Public order form → a **`pending_payment`** row in local Supabase (`story_type: story-4`, full inputs, photo in `order-photos/<id>/photo`, email) — **no generation, no charge**; `missing_favorite_activity` + `product_mismatch` + `invalid_email` reject server-side; `/api/checkout` 503 → graceful "we'll email a link" fallback (LS variant not provisioned). (4) Preview single-column (**6 `.preview-single`, 0 `.spread`**) with the real Otis cover + page-4; **Download → `If-Otis-Could-Talk.pdf`** (6 pages, Letter, `%PDF-1.4`); inline `ownerNames` edit re-rendered + persisted; regenerate scoped to the 2 slots (by construction — live paid repaint skipped per cost discipline). (5) Story-1 (14 imgs, `Saying-Goodbye-to-Otis.pdf`, pronoun+style fields intact) + Story-2 (2 imgs, `Letter-from-Murphy.pdf`) unchanged. Cleanup complete; canonical `feat21-talk-otis` / `b41b8df0` / `dc34eb11` fixtures intact; the cloud project never touched; both dev servers stopped.

**Gates:** `npm run build` ✓ (`/books/story-4-talk` + `/order/story-4-talk` SSG; storefront static, operator dynamic; public graph engine-free) · `npm run test:run` ✓ (**1099 tests, +55** — `lib/session/draft.test.ts` Story-4 block incl. the living+memorial `resolveStory4` round-trip + dispatcher no-longer-throws; `/api/session` Story-4 validation branches disk-mocked; `lib/catalog/products.test.ts` registry-derived `illustrationCount` guard; the implementer's `lib/story/story4/editable-fields.test.ts` + `illustrationLabels.ts` Story-4 drift guard) · `npx tsc --noEmit` ✓ clean · public-boundary test ✓ · code review **PASS** · commerce security **PASS** · context audit **resolved** · `qa` **PASS** (5/5, ~$0.012).

**Milestone 8 (Story 4) complete — all three products now run the full craft + commerce loop end to end: landing picker → wizard / order → generation → preview → print-quality PDF → (paid) approve → delivery, by id with no infra fork.** **Manual go-live items still pending across the commerce build (not code):** the Lemon Squeezy store setup (now incl. a third manual-fulfilment product/variant + `LEMONSQUEEZY_VARIANT_STORY_4_TALK`), final Story-4 price confirmation, a live test-mode purchase, a live Resend send-test, and the deferred AI-honesty disclosure copy + privacy policy. **Out of scope (post-MVP, never built for any product):** the "family" relationship option, re-send / order-lookup flows. **Carried from PR-20 (still open, not this PR):** the authored memorial gap-fill copy flagged for the grief-counselor review. **Process note:** local Supabase only ([[supabase-local-verify]]); dev servers stopped before the final `npm run build` ([[dev-build-cache-conflict]]); one Low run per the standing cost rule ([[qa-low-tier-cost-control]]).

