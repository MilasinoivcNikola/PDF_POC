## 2026-06-13 — Story 6 (PR-25): Living-Tribute Text, Registration & Imagery

**Branch:** `feature/story6-text` → `main` (`6f0cb2a`, merge `dd4f9ba`) · Milestone 10 (Story 6 — "While You're Still Here, [PET_NAME]"), Phase 1 (PR 1 of 2) · Craft Area 1 (pdf-render-specialist — text/registry) + Craft Area 2 (ai-image-specialist — prompts + dispatch) · the **first narrative-layout new book since Story 1**.

Authored the **"While You're Still Here, [PET_NAME]"** text engine — an 8-page **living tribute**, a **present-tense** narrator speaking *to and about a pet who is still alive* (cover + page-1 dedication + pages 2–6 + back cover) — as resolvable data + a full registry entry, and generated its **8 living-tribute images** (a locked reference + 7 reference-anchored scenes). A `Story6Session` resolves to an 8-page `ResolvedStory` reusing **Story 1's `cover`/`dedication`/`narrative`/`love`/`back-cover` layouts wholesale** and renders a real PDF via `render:test`. The book is **not yet sellable** (catalog, PR 26) or **creatable** (wizard, PR 26) — this PR makes the engine *know* and *generate* the book. **Story 1, Story 2, Story 4 AND Story 5 PDFs stay byte-identical.**

- **Headline divergence from the recent letter books: this is Story 1's shape, not the letters'.** Narrative facing-page layout (not single-column), and imagery is a locked reference illustration + **7 reference-anchored brief-driven scenes** via `images.edit` (Approach A, `totalImages = slots + 1 = 8`), **not** the letters' 2-image total-=-slots shape with a figure-free `images.generate` wash. The heaviest imagery work since feature 07; pet-consistency across 7 pages was the real risk and **Approach A held** (no Approach B / `input_fidelity` escalation needed).
- **The memorial re-render is DROPPED entirely (PM, 2026-06-12).** No `deathType`/`beliefFrame` on the Story-6 types, no second-life conversion, and the `truth` (death) layout **never** appears — death is never named in the living path. The master template's memorial-conversion / `DEATH_TYPE` / `BELIEF_FRAME` / `truth`-layout sections were annotated DROPPED/out-of-scope (top banner + the production-checklist residuals the context audit caught) so a future agent won't re-introduce them.
- **Text engine — `lib/story/story6/{master-text,variants,merge,editable-fields,fixtures}.ts` (new namespace).** Present-tense single-tense engine + the `transitionFrame` toggle (`still-here` default ends Page 5 on gratitude with no future mention; `road-ahead` swaps only Page 5's final paragraph for one plain forward-looking sentence — finitude named once, no euphemism). `composeVariants6()` (compose-before-merge, each affected page built whole) also layers an `ageOrStage` band heuristic (very-senior → Page-2 append; younger-diagnosed → softened Page-5 lead, gratitude unchanged), species voice (cat/rabbit/bird on Pages 2–4; `other` → neutral), and `otherPetsInHome: yes` → a Page-4 append. `mergeStory6` reuses shared `clean`/`substitute`/`MergeError` + `cleanOptional`/`appendOptionalLines`; `STORY_6_LAYOUT` tags `tribute-cover`→`cover`, `tribute-page-1`→`dedication`, `tribute-page-2..4`→`narrative`, `tribute-page-5`/`6`→`love`, `tribute-back-cover`→`back-cover` (never `truth`). Required-no-fallback: `petName`, `breedColor`, `ageOrStage`, `ownerNames`, `favoriteRitual`, `favoriteActivity`; optional-with-fallback `stillLoves`/`quirks`; optional-omit `ownerMessage`/`nicknames`/`dateAdopted`/`favoriteSpots`/`sleepingSpot`.
- **Registration scaffolding.** `lib/session/types.ts` (`TransitionFrame`, `Story6Memories`/`Toggles`/`Draft`/`Session` reusing `Pet` in full — incl. pronoun + illustrationStyle, unlike the letters — `+ Owner`; `StoryType += "story-6"`, `WizardDraft += Story6Draft`); `lib/story/master-text.ts` (`Story6PageId` + `PageId` widened, existing per-product maps stay narrowed); `lib/story/story-6.ts` (`story6Definition` + `TRIBUTE_SCENE_PAGE_IDS` = 7 slots, in the **product module** never `lib/ai/*` so the public registry/catalog graph stays engine-free); registry + `STORY_6_STEPS` wizard-config entries; `lib/pdf/filename.ts` `tributePdfFilename` → `While-Youre-Still-Here-[PET_NAME].pdf`; `lib/session/storage.ts` `newStory6Draft` + overload. Union-widening fallout matched the Story 4/5 pattern: `lib/session/draft.ts` (`isStory6Draft`; dispatchers throw "not wired (PR 26)" for a Story-6 draft — genuinely unreachable, no route reaches it) + `app/(operator)/create/letter/page.tsx` (Story 6 excluded from the letter-memories narrowing — its wizard has no `letter` step).
- **Imagery (engine only) — `lib/ai/story6-prompts.ts` + `generate.ts` dispatch.** `buildStory6SlotPrompts` resolves the story once and maps each of the 7 `TRIBUTE_SCENE_PAGE_IDS` to a prompt built from the resolved page's `illustrationBrief` + the shared "maintain the pet's exact appearance" consistency clause — **all 7 `useReference: true`**, brief-driven (page text change ⇒ prompt change), path-independent of the variant. `generate.ts` adds a runtime-guarded `storyType === "story-6"` path (`generateStory6Illustrations` / `regenerateStory6Slot`) mirroring the Story-1 path: locked reference first, then 7 scenes anchored on `[photo, reference]` via `images.edit` through the bounded worker pool + `withRetry`; `images.generate` is never reached (no wash). `app/(operator)/api/generate-illustrations/route.ts` introduced `REFERENCE_ANCHOR_STORIES = {story-1, story-6}` so both report `slots + 1` (Story 6 → 8) while the letters stay `slots`; `manifestToImageMap` + the `regenerate-illustration` route slot union widened to admit the 7 `tribute-*` slots (excludes `reference` + `tribute-back-cover`). `lib/runtime/surface.boundary.test.ts` adds `lib/ai/story6-prompts` to the banned-engine list (non-vacuous).

**Story-1/2/4/5 byte-identity (the hard requirement) — PASS.** Independently re-verified by the code reviewer AND by the orchestrator in the qa step: rendered each existing fixture on-branch and confirmed the **raw** byte length matches the documented constants exactly (Otis/Story-1 **873889**, Story-4 living **114796**, Story-4 memorial **122588**, Story-5 letter-to **126422**; Story-2 Murphy **119398** during `start` — `pages-story2.tsx` untouched) with the timestamp-normalized SHA stable across renders. No shared `pages.tsx`/`pages-story2.tsx`/`styles.css`/`globals.css` or `PageLayout`/`renderPage` switch touched.

**Live QA (engine-level, not browser — no wizard yet; mirrors Story 4 PR 21 / Story 5 PR 23) — PASS, ~$0.05 (one Low run, the only spend).** Built a real `feat25-tribute-otis` Story6Session on the canonical Jack Russell photo (copied, not moved). Generated exactly **8** images (`reference.png` + 7 `tribute-*.png`) in ~107s — the reference carries its own photo-only hash, all 7 scenes share the `[photo, reference]` hash (→ all `images.edit`), each scene a distinct prompt hash (→ brief-driven). The orchestrator **independently inspected the PNGs with the Read tool**: the **same Jack Russell recognizable across all 7 scenes** (white body, the asymmetric brown/grey forehead-and-muzzle patch, brown ears, amber eyes, brown nose), the senior grey honored subtly, the dog **awake and content** on the `love` pages 5/6 (not eyes-closed/elegiac), people from-behind per the style guide, **no figure-free wash anywhere**. **$0 cache re-run** proven (bogus key → 0 API calls, byte-identical PNGs). The on-disk `feat25-tribute-otis` book is **left intact** for PR 26's $0 sample frames; canonical fixtures untouched.

**Gates:** `npm run build` ✓ (storefront still static/SSG; `/order/[productId]` SSGs only story-1/2/4/5 — Story 6 correctly NOT yet sellable) · `npm run test:run` ✓ (**1357 tests, +99** — text suites `lib/story/story6/{variants,merge,registry}.test.ts` +77 [full `transitionFrame × age-band × species × otherPets` matrix with zero surviving placeholders, present-tense/tense-leak guard, banned-phrase guard, death-never-named, `truth`-never-assigned, per-variant page isolation + fresh-copy, fallbacks/optional-omission, `MergeError`, brace-injection, `tributePdfFilename`, `STORY_6_LAYOUT` double-lock; imagery suites `lib/ai/story6-prompts.test.ts` + `generate.story6.test.ts` +22 from `start`: 7 slots all `useReference:true`, 8 generations all via `images.edit`, `images.generate` never called, default `low` + override, cache hit = 0 calls, **Story 1(14)/2(2)/4(2)/5(2) totals pinned**, `manifestToImageMap` admits the 7 `tribute-*` & excludes `reference`+`tribute-back-cover`]) · `npx tsc --noEmit` ✓ clean · public-boundary test ✓ · code review **PASS** (no blockers; all six load-bearing invariants verified empirically) · context audit **DRIFT FOUND → resolved on-branch** (the template's production-checklist still said memorial-conversion fields are "captured and stored" + a "unless this is a memorial re-render" clause — both struck to match the DROPPED banner) · `qa` **PASS** (8-image consistency + $0 re-run + byte-identity).

**Phase 1 complete — Story 6's text resolves end to end and its 8 living-tribute images generate.** Lever held in reserve (not needed): Approach B / `input_fidelity` if a future drift-prone pet needs it across the 7 scenes. **Decisions / flags carried forward (not blockers):** `deriveAgeBand` is a heuristic on free-text `ageOrStage` (degrades gracefully — worst case a slightly-wrong register, never a crash; PR 26 may promote it to an explicit wizard field); pages 5 & 6 are illustration slots but the `love` renderer shows no `<img>`, so 2 of the 7 generated scenes are never displayed in the PDF — a **pre-existing accepted property of the reused layout** (exact parity with Story 1's page-10), flagged for the PM to decide whether PR 26 trims them; the live Low run rendered pages 2 & 4 as multi-panel montages (generation variance, the per-page regenerate fixes it in-product); the authored species / other-pets / younger-diagnosed gap-fill copy is flagged for the grief-counselor review (same carry-forward as Story 4/5); the `render:test` CLI writes the local debug file with the Story-1 filename regardless of `storyType` (pre-existing PR-20 quirk; the production filename via `getStory().pdfFilename` → `While-Youre-Still-Here-[PET_NAME].pdf` is correct). Two deferred playbook notes (context audit): the `tribute-*` page-id-prefix convention and the `slots + 1` reference-anchor accounting are followed in code but undocumented in `new-book-playbook.md` — a later-branch one-liner. Deferred to **PR 26** (completes Story 6 / Milestone 10): the wizard UI (`tribute`/`tone` step pages) + public order form + catalog entry + storefront + committed sample frames (reuse this PR's on-disk `feat25-tribute-otis` book for $0), wiring the `draft.ts` Story-6 dispatchers that currently throw. **Process note:** built with no `next dev` on this repo ([[dev-build-cache-conflict]]); one Low run per the standing cost rule ([[qa-low-tier-cost-control]]).

