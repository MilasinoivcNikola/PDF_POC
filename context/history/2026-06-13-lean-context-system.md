## 2026-06-13 — Feature 27: Lean the Context System (history tiering · tests-as-truth · debt ledger)

**Branch:** `feature/lean-context-system` → `main` (`8acefc3`, merge `8ce2183`) · dev-process / docs · main thread (verified by `context-auditor`) · the first non-story, process-only feature.

A **docs-only, zero-information-loss** pass that makes the standing context cheaper to load and less drift-prone. Four threads, one concern — shrink what every session/agent reads, stop prose from duplicating what tests already guarantee, and give deferred work a durable home. **No application code touched** — `npm run build`, `npm run test:run` (1450), and `npx tsc --noEmit` all pass unchanged because nothing executable moved.

- **History tiered.** The 255 KB `context/history.md` was split into **39 per-entry archive files** under `context/history/` (`YYYY-MM-DD-slug.md`), each **byte-preserved** from the original; `history.md` is now a lean one-line-per-entry index grouped by milestone (M1–M10 + the Commerce build), every line carrying `` `branch` (sha) `` + a one-clause hook + an `→ [archive](…)` link. **Zero-loss verified two ways:** programmatically (`preamble + concat(39 files in index order) == git HEAD:context/history.md`, **byte-identical, 255,846 bytes**) and independently by the `context-auditor` (`cmp` of the reconstruction + a 39↔39 bijection of index links to files). Monotonic growth no longer taxes every session.
- **`CLAUDE.md` `@`-loads trimmed to the 5 live docs** (`coding-standards`, `ai-interaction`, `commerce-roadmap`, `current-feature`, `history`). Demoted to plain "load on demand" path references — content untouched: the 4 `prototypes/*.html`, `saying-goodbye-to-otis.pdf`, the completed-story masterstories (story-1/2/3), **and** the superseded `local-prototype-plan.md`. A note records that the *in-progress* story's masterstory gets re-promoted to `@`-load for the duration of its milestone. (Demoting the superseded plan beyond the spec's literal demote-list was a deliberate call to honor the spec's twice-stated "five live docs"; the auditor confirmed it consistent with intent — "leave as historical" governs the plan's *content*, which didn't change.)
- **Tests as source of truth** in `coding-standards.md`. The big *Deploy-surface boundary* block now keeps every decision (fail-closed `operator` default, the per-request `force-dynamic` page gate, the three-tier model, "ships as gated 404 not build-excluded") but defers the per-PR route inventory to `lib/runtime/surface.boundary.test.ts` + `all-operator-routes-gate.test.ts`; the order state-machine line points at `lib/order/state.test.ts`; the byte-identity rule and the "no literal `[FIELD]` survives" rule point at the template / `merge.test.ts` suites. Only the test-restating "what-it-verifies" prose was removed — all rationale retained (the boundary block actually *gained* the fail-closed explanation).
- **Debt ledger.** New `context/debt.md` — a triage table (item · area/files · why-deferred · severity · flips-to-blocking-when), seeded with **17 rows** harvested from the scattered "carried forward / not a blocker" notes (privacy policy + Lemon Squeezy store setup as `high` launch blockers; AI-honesty copy, grief-counselor review, the reused-route operator-auth boundary as `medium`; the worker two-process race, `failed→queued` re-spend, `STYLE_PHRASE`/`cleanOptional` dup, Story-6 unused love-page slots, playbook doc gaps, alternate sizes, warm Puppeteer pool, Stories 2+5 bundle as `low`). The auditor confirmed all 17 trace to a real archive carry-forward (no invented debt).
- **Convention wired in three consistent places** so the next `/feature complete` keeps the index lean instead of pasting a monolithic block into it: `history.md`'s header note, `ai-interaction.md` step 10, and — added at Nikola's prompt during review — `.claude/skills/feature/actions/complete.md` step 6 (full write-up → `context/history/`; one index line → `history.md`; durable deferrals → `debt.md`). `context/features/README.md` gained rows 27 + 28 (28 = the optional follow-up to *auto*-append the ledger from the skill, still planned).

**Gates:** `npm run build` ✓ (Compiled successfully; storefront still static/SSG, operator dynamic) · `npm run test:run` ✓ (**1450 passed / 73 files, unchanged** — no executable touched) · `npx tsc --noEmit` ✓ clean · public-boundary test still green · **`context-auditor` IN SYNC** (the designated gate — zero info loss, every trimmed claim still test-enforced, no actively-needed doc dropped, `debt.md` faithful) · `code-reviewer` **PASS** (docs-only; verified the mechanical claims — no code touched, gates unchanged, test pointers accurate at `file:line`, 39↔39 link/file bijection). No `test-author` (nothing executable changed) and no `qa` (nothing user-facing renders).

**This entry is the first logged under the new tiered procedure** — written as its own archive file with a single index line under the new `## Dev-process & tooling` heading, dogfooding the `complete.md` step 6 it added. Deferred: **feature 28** — auto-append the index line + debt rows from the `/feature` skill (the manual convention is now documented; automation is the optional next step). No durable debt introduced by this feature.
