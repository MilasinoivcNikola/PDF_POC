## 2026-06-11 — Commerce PR-10: New-Book Product Playbook (recipe formalized)

**Branch:** `feature/new-book-playbook` → `main` (`2e3d450`, merge `b85cddd`) · Phase 5 (pdf-render-specialist)

Commerce build, PR-10 — **the final commerce PR, Phase 5.** Formalized the "build more books and sell only them" path into a standing, repo-checked recipe. **Documentation only — no product code, no new dependency, and no book authored** (the which-title call — Story 3 vs. a fresh original — was PM-deferred at `load`: PM chose "formalize the recipe only"). The pipeline is already product-agnostic (features 14–19) and the whole order → pay → worker → admin → delivery loop (PRs 01–09) is reused unchanged, so a new title is mostly **authoring**, not plumbing; this PR writes down exactly how, at file-path granularity, so the next book is a quick low-risk branch rather than a rediscovery.

- **New:** `context/new-book-playbook.md` — the canonical recipe a future `feature/book-<name>` branch follows. Sections: *Before you start* → **Steps 1–6** (1: author text in `lib/story/<book>/{master-text,variants,merge}.ts` reusing `clean`/`substitute`/`MergeError` from `lib/story/merge.ts`, compose-before-merge, honour the template's "died"/banned-phrase quality bar; 1a: extend the `PageId` union; 2: add the `StoryType` value + a `StoryDefinition` in `lib/story/registry.ts` — `resolve`/`illustrationSlots`/`pdfFilename`/`wizard`/`editable` — plus the draft→session/wizard wiring; 3: layout/CSS **only if** a new `PageLayout` — the exhaustive no-`default` `renderPage` switch, screen↔PDF parity, the feature-16 `letter` byte-identity precedent; 4: a `Product` in `lib/catalog/products.ts` with `illustrationCount` **derived** from the registry + the client-safe `lib/story/scenes.ts`-not-`lib/ai/*` boundary; 5: the `LEMONSQUEEZY_VARIANT_<PRODUCT_ID>` env var, manual fulfilment; 6: Low-tier samples) → *Standing guards* (own merge/variant tests with zero surviving placeholders, existing-book PDF **byte-identity** via length + timestamp-normalized SHA, `build`/`test:run`/`tsc` green, the public-boundary test) → *Reuse guarantee* (order→delivery flows **by id**, `orderId === sessionId`, zero infra changes) → an explicitly-illustrative Story-3 skeleton (labelled **not** a committed book).
- **Edited:** `context/commerce-roadmap.md` — a one-line forward link to the playbook from the Phase 5 ("Build more books") bullet. `CLAUDE.md` deliberately untouched (it loads into every session — kept lean; the roadmap link is enough).

**Accuracy bar (the whole point of a recipe doc) — met.** Every cited file path / export / type / env var / script was verified against the current tree by the implementer **and independently re-verified by the context auditor** (registry shape + `getStory`, `lib/pdf/filename.ts`, `buildProduct`/derived `illustrationCount`, `formatPriceUsd`, the full `wizard-config.ts` surface, `scenes.ts`, `merge.ts` primitives, the exhaustive `renderPage` switch, the env convention, the `(public)`/`(operator)` route-group paths, the worker reuse, the byte-identity method, `render:test` + `fixtures/otis.json`). Corrected one stale assumption from the older spec/plan: the landing story picker is at `app/(public)/page.tsx` (PR-03 split), not `app/page.tsx`.

**Gates:** `npm run build` ✓ (storefront still static/SSG, operator dynamic — boundary intact) · `npm run test:run` ✓ (**895 tests, unchanged** — docs add nothing testable; no `test-author` dispatch) · `tsc --noEmit` ✓ · context audit **IN SYNC** (every citation verified; no drift; roadmap link clean). `code-reviewer` + `commerce-security-reviewer` **not dispatched** — the diff is `context/*.md` only, zero code/commerce surface. No `qa` (nothing user-facing renders).

**Commerce build complete — PRs 01–10 all landed.** PR-10 is **recurring**, not one-and-done: each future title is its own small branch following this recipe, not another infra PR. Candidate next titles: pure originals against existing layouts (lightest), or **Story 3 — Life Story booklet** (template authored, but photo-heavy with new bio-card/timeline/photo layouts + a longer wizard → its own multi-feature milestone, **not** the lightweight playbook). **Manual go-live items still pending across the commerce build (not code):** the Lemon Squeezy store setup (per-book manual-fulfilment products/variants + the 5 env vars + webhook endpoint + Serbian payout), a live Resend send-test, and the deferred AI-honesty disclosure copy + privacy policy.

